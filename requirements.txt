# OpticEats — Python Dependencies
# Install with: pip install -r requirements.txt

# ── Stage 1: OCR ──────────────────────────────────────
easyocr>=1.7.0
pillow>=10.0.0
opencv-python-headless
numpy>=1.24.0

# ── Stage 2: Parser ───────────────────────────────────
# (pure Python — no extra installs needed)

# ── Stage 3: Classifier ───────────────────────────────
# Optional LLM fallback:
# anthropic>=0.25.0

# ── Stage 4: Scorer ───────────────────────────────────
# (pure Python — no extra installs needed)

# ── Stage 5: Streamlit App ────────────────────────────
streamlit>=1.32.0